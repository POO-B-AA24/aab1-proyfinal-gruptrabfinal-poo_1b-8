package View;

import Model.*; // Importa todas las clases del paquete Model

// Define la clase Ejecutor que contiene el punto de entrada principal del programa
public class Ejecutor {

    // Método principal que se ejecuta al iniciar la aplicación
    public static void main(String[] args) {
        // Crea una instancia de DatosBuses
        DatosBuses app = new DatosBuses();
        app.menu();
        // Llama al método menu de DatosBuses para iniciar la interacción con el usuario
    }
    public void menu(){
        
    }
}
